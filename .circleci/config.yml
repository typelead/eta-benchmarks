version: 2.0
jobs:
  build:
    docker:
    - image: typelead/eta:latest
    environment:
      JAVA_OPTS: -Xmx1.5G -Xms1.5G
    steps:
    - checkout
    - run: ./init.sh
    - run: ./fast-benchmarks.sh
    - run: ./slow-benchmarks.sh

workflows:
  version: 2
  full_cycle:
    jobs:
      - build
